module Tristan.Community.ChessColiseum.Context;

import Tristan.Community.ChessColiseum.Event.MoveBegin;
import Tristan.Community.ChessColiseum.Event.MoveEnd;
import Tristan.Community.ChessColiseum.Event.MoveSubmitted;
import Tristan.Community.ChessColiseum.Event.MakeMove;

obj Move is Context {
    meta {
        let String NormalName = "Move";
        let Int32 Id = 1301;
        let Type Key = Tuple[UInt32];
        let Type BeginEvent = MoveBegin;
        let Type EndEvent = MoveEnd;
        let Type Events = TypeSet[MoveSubmitted, MakeMove];
        let Type Children = TypeSet[];
    }
}
