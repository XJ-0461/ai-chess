module Tristan.Community.ChessColiseum.Event;

import Tristan.Community.ChessColiseum.Type.Color;

obj MoveSubmitted is Event {
    meta {
        let String NormalName = "Move Submitted";
        let Int32 Id = 1205;
        let Array[String, 2] NormalFieldNames = Array[String, 2].new("Player", "Move");
    }

    fields {
        let Color player = Color.new(0);
        let String move = "";
    }
}
