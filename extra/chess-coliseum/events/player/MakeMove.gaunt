module Tristan.Community.ChessColiseum.Event;

obj MakeMove is Event {
    meta {
        let String NormalName = "Make Move";
        let Int32 Id = 1103;
        let Array[String, 2] NormalFieldNames = Array[String, 2].new("Move", "Validation");
    }

    fields {
        let String move = "";
        let String validation = "";
    }
}
